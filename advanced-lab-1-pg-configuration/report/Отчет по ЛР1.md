Университет ИТМО
Факультет программной инженерии и компьютерной техники
___
Группа: P3311
Студенты: Горошников Т.И., Ибрагимова А.И.
Преподаватель: Заболотняя О.М.
# <u><center>**Отчет по лабораторной работе №1**</center></u>
## <center>**Вариант 408476**</center>
<div class="page-break" style="page-break-before: always;"></div>

## Задание
### Этап 1. Инициализация кластера БД
- Директория кластера: `$HOME/ajm59`
- Кодировка: UTF8
- Локаль: английская
- Параметры инициализации задать через аргументы команды
### Этап 2. Конфигурация и запуск сервера БД
- Способы подключения: 1) Unix-domain сокет в режиме peer; 2) сокет TCP/IP, принимать подключения к любому IP-адресу узла
- Номер порта: `9476`
- Способ аутентификации TCP/IP клиентов: по имени пользователя
- Остальные способы подключений запретить.
- Настроить следующие параметры сервера БД:
    - max_connections
    - shared_buffers
    - temp_buffers
    - work_mem
    - checkpoint_timeout
    - effective_cache_size
    - fsync
    - commit_delay
  Параметры должны быть подобраны в соответствии со сценарием OLAP:  
  10 одновременных пользователей, пакетная запись/чтение данных по 64МБ.
- Директория WAL файлов: `$PGDATA/pg_wal`
- Формат лог-файлов: `.csv`
- Уровень сообщений лога: `ERROR`
- Дополнительно логировать: завершение сессий и продолжительность выполнения команд
### Этап 3. Дополнительные табличные пространства и наполнение базы
- Создать новые табличные пространства для временных объектов: `$HOME/hit89, $HOME/tdg81`
- На основе template1 создать новую базу: `fatredexam`
- Создать новую роль, предоставить необходимые права, разрешить подключение к базе.
- От имени новой роли (не администратора) произвести наполнение ВСЕХ созданных баз тестовыми наборами данных. ВСЕ табличные пространства должны использоваться по назначению.
- Вывести список всех табличных пространств кластера и содержащиеся в них объекты.
## Ход работы
### Этап 1
- Инициализация кластера:
  `initdb -D ../ajm59 -E UTF8 --locale=en_US.UTF-8`
- Запуск кластера:
  `postgres -p 9476 -D /var/db/postgres2/ajm59/ &`
- Создание базы данных:
  `createdb -p $PGPORT`
- Подключение к БД:
  `psql -p 9476`
### Этап 2
Скрипт для конфигурации параметров кластера:
```
ALTER SYSTEM SET MAX_CONNECTIONS=10;
ALTER SYSTEM SET SHARED_BUFFERS='1GB';
ALTER SYSTEM SET TEMP_BUFFERS='8MB';
ALTER SYSTEM SET WORK_MEM='64MB';
ALTER SYSTEM SET CHECKPOINT_TIMEOUT='15min';
ALTER SYSTEM SET EFFECTIVE_CACHE_SIZE='3GB';
ALTER SYSTEM SET FSYNC=ON;
ALTER SYSTEM SET COMMIT_DELAY=0;

ALTER SYSTEM SET LOGGING_COLLECTOR=on;
ALTER SYSTEM SET LOG_DESTINATION='csvlog';
ALTER SYSTEM SET LOG_DIRECTORY='log';
ALTER SYSTEM SET LOG_FILENAME='postgresql-%Y-%m-%d_%H%M%S.csv';
ALTER SYSTEM SET LOG_MIN_MESSAGES=error;

ALTER SYSTEM SET LISTEN_ADDRESSES='*';
ALTER SYSTEM SET UNIX_SOCKET_DIRECTORIES='/tmp';
```
Измененные строки в `pg_hba.conf` для конфигурации способов подключения к БД:
```
# "local" is for Unix domain socket connections only
local   all             all                                     peer
# IPv4 local connections:
host    all             all             0.0.0.0/0               scram-sha-256
# IPv6 local connections:
host    all             all             ::/0                    scram-sha-256
```
### Этап 3
- Создание директорий для табличных пространств и выдача прав:
```
mkdir ~/hit89
mkdir ~/tdg81

chmod 700 ~/hit89
chmod 700 ~/tdg81
```

- Привязка табличных пространств к созданным директориям:
```sql
DROP TABLESPACE IF EXISTS hit89;
DROP TABLESPACE IF EXISTS tdg81;

CREATE TABLESPACE hit89 LOCATION '$HOME/hit89';
CREATE TABLESPACE tdg81 LOCATION '$HOME/tdg81';
```

- Создание новой базы данных на основе template1:
```sql
DROP DATABASE IF EXISTS fatredexam;
CREATE DATABASE fatredexam TEMPLATE template1;
```

- Создание новой роли с правами:
```sql
DROP ROLE IF EXISTS labuser;
CREATE ROLE labuser WITH LOGIN PASSWORD 'labpass';

GRANT CONNECT ON DATABASE fatredexam TO labuser;
GRANT CREATE ON TABLESPACE hit89 TO labuser;
GRANT CREATE ON TABLESPACE tdg81 TO labuser;

ALTER ROLE labuser SET temp_tablespaces = 'hit89,tdg81';
```

- Настройка прав на схему public в базе fatredexam:
```sql
GRANT ALL ON SCHEMA public TO labuser;
GRANT CREATE ON SCHEMA public TO labuser;
ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT ALL ON TABLES TO labuser;
ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT ALL ON SEQUENCES TO labuser;
```

- Создание таблиц от имени роли labuser с использованием разных табличных пространств:
```sql
CREATE TEMPORARY TABLE sales (
    id SERIAL,
    amount INT
);

CREATE TEMPORARY TABLE customers (
    id SERIAL,
    name TEXT
);

CREATE TEMPORARY TABLE products (
    id SERIAL,
    name TEXT
);
```

- Наполнение таблиц тестовыми данными:
```sql
INSERT INTO sales (amount) VALUES (100), (250), (500);
INSERT INTO customers (name) VALUES ('Ivan'), ('Anna'), ('Petr');
INSERT INTO products (name) VALUES ('Phone'), ('Laptop'), ('Tablet');
```

- Проверка размещения таблиц по табличным пространствам:
```sql
SELECT tablename, tablespace 
FROM pg_tables 
WHERE schemaname = 'public';
```

Результат:
```
Таблицы и их tablespace:
 tablename | tablespace
-----------+------------
 sales     | hit89
 customers | tdg81
 products  |
(3 строки)
```

- Вывод списка всех табличных пространств кластера:
```sql
\db+
```

Результат:
```
                                         Список табличных пространств
    Имя     | Владелец  |      Расположение       |     Права доступа     | Параметры |   Размер   | Описание
------------+-----------+-------------------------+-----------------------+-----------+------------+----------
 hit89      | postgres2 | /var/db/postgres2/hit89 | postgres2=C/postgres2+|           | 8195 bytes |
            |           |                         | labuser=C/postgres2   |           |            |
 pg_default | postgres2 |                         | postgres2=C/postgres2 |           | 30 MB      |
 pg_global  | postgres2 |                         |                       |           | 605 kB     |
 tdg81      | postgres2 | /var/db/postgres2/tdg81 | postgres2=C/postgres2+|           | 16 kB      |
            |           |                         | labuser=C/postgres2   |           |            |
(4 строки)
```